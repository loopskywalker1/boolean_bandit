if outputFileType == "infer":
    outputFileType = 'json' if has_nested_data(df) else 'tsv'

if mode == "display":
    print(response_data)
elif mode == "j-drive":
    f = getJDriveFile(file_path=outputFilename, mode="w", endpoint=endpoint, file_extension=outputFileType)
else:
    f = getLocalFile(endpoint=endpoint, outputFilename=outputFilename, file_extension=outputFileType)

if outputFileType == 'json':
    # Change from df.write_ndjson() to df.write_json() for proper JSON format
    content = df.write_json()  # This should be df.write_json(), not ndjson
    f.write(content)
    
    # Add this section to create NDJSON file in the ndjson subfolder
    if mode == "j-drive":
        ndjson_dir = os.path.join(JDriveResources["Path"], "ndjson")
        os.makedirs(ndjson_dir, exist_ok=True)
        ndjson_path = os.path.join(ndjson_dir, f"{endpoint}_Response.ndjson")
        with open(ndjson_path, "w") as ndjson_file:
            ndjson_content = df.write_ndjson()
            ndjson_file.write(ndjson_content)
    else:
        ndjson_dir = "query_results/ndjson"
        os.makedirs(ndjson_dir, exist_ok=True)
        ndjson_path = os.path.join(ndjson_dir, f"{endpoint}_Response.ndjson")
        with open(ndjson_path, "w") as ndjson_file:
            ndjson_content = df.write_ndjson()
            ndjson_file.write(ndjson_content)
    
elif outputFileType == 'tsv':
    content = df.write_csv(separator='\t')
    f.write(content)
elif outputFileType == 'avro':
    df.write_parquet('temp.parquet')
    # avro writing bug workaround
    pl.read_parquet("temp.parquet").write_avro(f)
elif outputFileType == 'parquet':
    df.write_parquet(f)
f.close()
