if outputFileType == "infer":
    # Determine if we should use JSON format
    if has_nested_data(df):
        # Main file as JSON
        outputFileType = "json"
        
        # Write the primary JSON file
        if mode == "j-drive":
            f = getJDriveFile(file_path=outputFilename, mode="w", endpoint=endpoint, file_extension=outputFileType)
        else:
            f = getLocalFile(endpoint=endpoint, outputFilename=outputFilename, file_extension=outputFileType)
        
        # Write standard JSON
        df.write_json(f)
        f.close()
        
        # Create the secondary NDJSON file
        if mode == "j-drive":
            # Create path to ndjson subfolder
            ndjson_path = f"{JDriveResources['Path']}/ndjson/{endpoint}_Response.ndjson"
            # Ensure directory exists
            os.makedirs(os.path.dirname(ndjson_path), exist_ok=True)
            # Write NDJSON file
            df.write_ndjson(ndjson_path)
        else:
            # For local mode
            ndjson_path = f"query_results/ndjson/{endpoint}_Response.ndjson"
            # Ensure directory exists
            os.makedirs(os.path.dirname(ndjson_path), exist_ok=True)
            # Write NDJSON file
            df.write_ndjson(ndjson_path)
        
        print(f"Created JSON file and NDJSON file in {os.path.dirname(ndjson_path)}")
    else:
        # No nested data, use TSV
        outputFileType = "tsv"
        if mode == "j-drive":
            f = getJDriveFile(file_path=outputFilename, mode="w", endpoint=endpoint, file_extension=outputFileType)
        else:
            f = getLocalFile(endpoint=endpoint, outputFilename=outputFilename, file_extension=outputFileType)
        
        content = df.write_csv(f, separator='\t')
        f.close()
elif outputFileType == "json":
    # Just JSON file requested
    if mode == "j-drive":
        f = getJDriveFile(file_path=outputFilename, mode="w", endpoint=endpoint, file_extension=outputFileType)
    else:
        f = getLocalFile(endpoint=endpoint, outputFilename=outputFilename, file_extension=outputFileType)
    
    df.write_json(f)
    f.close()
elif outputFileType == "ndjson":
    # Just NDJSON file requested
    if mode == "j-drive":
        f = getJDriveFile(file_path=outputFilename, mode="w", endpoint=endpoint, file_extension=outputFileType)
    else:
        f = getLocalFile(endpoint=endpoint, outputFilename=outputFilename, file_extension=outputFileType)
    
    df.write_ndjson(f)
    f.close()
# [Keep your existing code for other formats]
